spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: ${KANGDORA_DB_URL}
    username: ${KANGDORA_DB_USERNAME}
    password: ${KANGDORA_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
